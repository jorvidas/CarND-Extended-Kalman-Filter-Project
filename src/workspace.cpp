#include "Eigen/Dense"
#include <iostream>
#include <cmath>

using namespace std;
using Eigen::MatrixXd;
using Eigen::VectorXd;

int main() {
  // VectorXd x_(4); 
  // VectorXd x_ <<1, 2, 1, 2;
  // cout<<"Is this working?\n";
  // cout<<x_[0]<<endl;

  // float px = x_[0];
  // float py = x_[1];
  
  // float rho = sqrt(pow(px,2)+pow(py,2));
  // float phi = atan(px/0.001);
  // float rhoDot = ((px*x_[2]+py*x_[3])/rho);

  // VectorXd hOfX(3);
  // hOfX<<rho,phi,rhoDot;

  // cout<<hOfX;

  cout<<M_PI;

return 0;

}


